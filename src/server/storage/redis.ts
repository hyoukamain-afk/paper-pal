/**
 * Redis adapter interface — production implementation would use Upstash Redis.
 * MVP uses in-memory store via createMemoryRedis() in memory-store.ts.
 */
export type { RedisAdapter } from "./types";
